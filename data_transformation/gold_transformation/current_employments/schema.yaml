version: "1.0"

columns:
  - name: glynac_organization_id
    type: LowCardinality(String)
    nullable: false
    description: "Organization identifier"

  - name: current_employment_count
    type: UInt64
    nullable: false
    description: "Number of contacts with current employment in the organization"