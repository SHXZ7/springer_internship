version: "1.0"

columns:
  - name: glynac_organization_id
    type: LowCardinality(String)
    nullable: false
    description: "Organization identifier"

  - name: contacts_with_phone
    type: UInt64
    nullable: false
    description: "Number of contacts with at least one phone number"